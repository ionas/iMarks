<!DOCTYPE html>
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
		<title>iMarks &mdash; Save bookmarks in your own cloud.</title>
		<%= stylesheet_link_tag 'base' %>
		<%= javascript_include_tag 'jquery.min', 'rails', 'application' %>
		<%= javascript_include_tag 'pagination' %>
		<%= csrf_meta_tag %>
	</head>
	<body>
		<%= yield %>
		<% if user_signed_in? %>
		  You are signed in as <strong><%= current_user.email %></strong> &mdash; <%= link_to "Sign out", destroy_user_session_path %>?
		<% else %>
	    <a href="/users/sign_up">Sign up</a> or <%= link_to "sign in", new_user_session_path %>.
		<% end %>
	</body>
</html>